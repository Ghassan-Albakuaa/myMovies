//export default function SearchByMovieTranscript(movies){
/*
    movies.map(element => {                     
         
        if (element.transcript.includes(localStorage.movie__transcript))
        {   
          
           sessionStorage.setItem("TMovieId", element.id); 
         
        }
        if (element.transcript.includes(localStorage.Movie___Name))
        {   
          alert("yes");
           sessionStorage.setItem("SMovieId", element.id); 
         
        }
        }).join("");       



    ////////////////////////////////////////
    var MovieList =[];
      movies.map(element => {                     
           
         
          if (element.transcript.includes(localStorage.Movie___transcript))
          {   
           alert(element.transcript);
           alert(element.title);
           //  sessionStorage.setItem("TMovieId", element.id); 
           MovieList.push(element.title);
/*
return `
<h1> m m mm m</h1>
<section class="movie__search_section"><div>${element.title}${element.link}${element.transcript}</div></section>
`
 
          }
          }).join("");       
      
      return `
      
      <table border="10px><tr>
${MovieList.map(function (value){
    
    <td>${value}</td>
    
}).join("")}
</table>
            
      ` 
  */        

//}


 